# SauceDemoFramework

Steps to be followed 
1. pip install -r requirements.txt
2. playwright install
3. pytest tests/test_checkout.py
4. pytest --alluredir=allure-results 
5. allure serve allure-results
